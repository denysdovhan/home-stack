  plex:
    image: linuxserver/plex
    container_name: plex
    network_mode: host
    environment:
      - PUID=1000
      - PGID=1000
      - VERSION=docker
    volumes:
      - ${VOLUMES}/plex/config:/config
      # - ${MEDIA}/media:/media
    restart: unless-stopped
